## Kaiko Platform Documentation

## Building the documentation

The environment necessary to build the documentation is fully dockerized: use `$ make build`.

A live local server is also available using `$ make server`.
